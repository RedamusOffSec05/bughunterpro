[pytest]
testpaths = tests
addopts =
    --cov=BugHunterPro
    --cov=ratelimit
    --cov=config
    --cov=reporter
    --cov=modules
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-report=json:coverage.json
    -v
